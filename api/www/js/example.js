$(function () {
    // prevent standard form submission
    $('#search-form').submit(function (event) {
        event.preventDefault();

        // TODO: perform search
        alert("Search is not currently implemented.");
    });
});